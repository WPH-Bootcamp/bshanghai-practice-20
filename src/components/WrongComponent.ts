import { useState } from "react";

function regularFunction() {
  const [count, setCount] = useState(0); //salah
}

export default regularFunction;

// gunakan .ts untuk menyimpan type, interface, generic functino
// gunakan .tsx untuk menyimpan react component/custom hook
